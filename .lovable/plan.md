Aumentar o tamanho do texto "ESTUDO DE MERCADO" no header da landing page (`src/routes/index.tsx`).

Mudança em `src/routes/index.tsx` linha 39:
- De: `text-[10px] font-semibold uppercase tracking-wider`
- Para: `text-lg font-bold uppercase tracking-wider` (com remoção do span vazio acima para não sobrar espaço em branco)